@startuml

interface "System.IComparable" as IComparable
IComparable <|.. BusStop

class BusStop {
 - _nr: int
 - _name: string
 - _note: string
 - NrOfComparisons : int {static}
 + BusStop(nr : int, name : string, note : string = "-")
 + ToString() : string <<override>>
 + CompareTo(other : BusStop?) : int
 + GetNr() : int
 + GetName(): string
 + GetNote() : string
 + GetCopy() : BusStop
 + ResetNrOfComparisons(): void {static}
 + GetNrOfComparisons() : int {static}
}

interface IComparable<T> {
 CompareTo(other : T?) : int
}

@enduml